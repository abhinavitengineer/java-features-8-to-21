spring: 
  datasource: 
    url: jdbc:h2:mem:testdb 
    driverClassName: org.h2.Driver 
    username: sa 
    password: 
  h2: 
    console: 
      enabled: true
mybatis:
  type-aliases-package: com.example.finance.model
  mapper-locations: classpath:mapper/*.xml

strategy:
  matrix:
    java: [ 8, 17 ]

springdoc:
  swagger-ui:
    path: /swagger-ui.html
